#pragma once
#include<iostream>
#include<vector>
#include<string>
using namespace std;
#include<stdlib.h>

string PrintMinNumber(vector<int> numbers) 
{
	if (numbers.empty())
		return NULL;
	vector<int>::iterator it = numbers.begin();
	string s;

}